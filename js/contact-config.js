// Configure same-origin proxies when the integrations are ready. Never place secrets in browser code.
// endpoint: enquiry forms. orderEndpoint: checkout orders. reviewEndpoint: product reviews (multipart, with photos).
window.COMPRINT_CONTACT = Object.freeze({ endpoint: null, orderEndpoint: null, reviewEndpoint: null, email: 'sales@comprint.co.in' });
